server:
  port: 8080

spring:
  application:
    name: personal-web-ai

  datasource:
    url: ${DB_URL:${SPRING_DATASOURCE_URL}}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      base-url: ${OPENAI_BASE_URL}
      chat:
        options:
          model: ${OPENAI_CHAT_MODEL}
          temperature: 0.2
      embedding:
        options:
          model: ${OPENAI_EMBEDDING_MODEL}

    vectorstore:
      type: chroma
      chroma:
        client:
          host: http://${CHROMA_HOST:10.16.11.197}
          port: ${CHROMA_PORT:8001}
        collection-name: ${CHROMA_COLLECTION_NAME:personal-portfolio}
        tenant-name: ${CHROMA_TENANT_NAME:default_tenant}
        database-name: ${CHROMA_DATABASE_NAME:default_database}
        initialize-schema: false

app:
  admin:
    api-key: ${ADMIN_API_KEY}
  rag:
    top-k: 5
    similarity-threshold: 0.3
    auto-ingest-mock: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

spring.codec.max-in-memory-size: 10MB
spring.servlet.multipart.max-file-size: 10MB
spring.servlet.multipart.max-request-size: 10MB
